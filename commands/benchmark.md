---
description: 生成 Computer Use 能力评分报告
argument-hint: [--output REPORT.json|REPORT.md]
---

运行 `python -m reasonix_computer_use.capability_runner benchmark $ARGUMENTS`，汇总完成率、工具调用、
视觉次数、重复动作和耗时。不要把一次真实应用成功当作发布结论。
