---
description: 运行 Computer Use 通用能力测试
argument-hint: [quick|full] [--app PATH]
---

运行 `python -m reasonix_computer_use.capability_runner $ARGUMENTS`。未提供参数时使用 `quick`。
只报告通用能力结果，不操作真实用户应用；在线模式只接受 Reasonix CapabilityApp。
